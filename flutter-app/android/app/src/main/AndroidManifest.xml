<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Add internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- libre_location ships a full background-tracking manifest. We only use a
         one-shot foreground fix ("Mein Standort"), so strip everything it merges
         in beyond ACCESS_FINE/COARSE_LOCATION. Re-enable the specific lines below
         if/when we add a real background-tracking feature. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" tools:node="remove" />
    <uses-permission android:name="android.permission.WAKE_LOCK" tools:node="remove" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" tools:node="remove" />
    <!-- "Mein Standort" on the Bahnhofskarte: show the user's position and the
         direction toward their boarding Gleis. Coarse + fine; while-in-use. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- "Split-Ticket-Analyse fertig" notification (Android 13+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Trip reminders: schedule exact wall-clock alarms ("30 Min vorher") and
         re-arm them after a reboot. USE_EXACT_ALARM is the non-revocable grant
         for an app whose reminders are a core function; SCHEDULE_EXACT_ALARM is
         the runtime-requested fallback. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Ankunfts-Wecker: show the loud arrival alarm full-screen on the lock
         screen (Android 14+ gates this; degrades to a heads-up banner if the
         OS withholds the grant). VIBRATE for the gentle "in 10 Min" ping. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:label="Besser Bahn"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- Drop libre_location's background components (we only do one-shot fixes). -->
        <service android:name="io.rezivure.libre_location.LocationService" tools:node="remove" />
        <receiver android:name="io.rezivure.libre_location.BootReceiver" tools:node="remove" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- OAuth callbacks for the Android classic-Custom-Tab bridge:
             - besserbahn:// — Träwelling login (bahn.chuk.dev bounce page).
             - dbnav://     — Deutsche-Bahn account login; DB's Keycloak
                              redirects straight into the DB Navigator app
                              scheme, which we capture here.
             - bahnbonus:// — separate BahnBonus OAuth token for the official
                              personal CO₂ statistics service.

             Version 5 of flutter_web_auth_2 uses Chrome Auth Tab, which only
             closes reliably with these custom schemes on recent browsers. The
             local bridge deliberately uses a classic Custom Tab and brings its
             management activity forward with CLEAR_TOP after the redirect. This
             closes the browser on old and new Chrome versions without using an
             ephemeral session (and therefore keeps DB login cookies). All three
             activities use the empty task affinity so the callback can reach the
             waiting activity in the same task (#35). -->
        <activity
            android:name=".OAuthManagementActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity
            android:name=".OAuthCallbackActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:taskAffinity="">
            <intent-filter android:label="Besser Bahn OAuth">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="besserbahn" />
                <data android:scheme="dbnav" />
                <data android:scheme="bahnbonus" />
            </intent-filter>
        </activity>
        <!-- flutter_local_notifications: deliver scheduled reminders and re-arm
             them after a device reboot. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Declares Flutter v2 embedding. Required; do not remove. -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
